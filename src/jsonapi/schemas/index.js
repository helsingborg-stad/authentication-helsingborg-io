const auth = require('./auth.jsonapi');
const collect = require('./collect.jsonapi');
const sign = require('./sign.jsonapi');
const cancel = require('./cancel.jsonapi');

module.exports = {
  auth,
  collect,
  sign,
  cancel,
};
